OPENAI_API_KEY=sk-your-key-here

# Prefer free/advanced daily quota model first (override if your offer uses another ID)
OPENAI_PRIMARY_MODEL=gpt-5.6-luna

# Optional mid-tier before paid mini
# OPENAI_SECONDARY_MODEL=gpt-4.1-mini

# Paid wallet fallback — cheapest capable (protects your $5)
OPENAI_FALLBACK_MODEL=gpt-4o-mini
